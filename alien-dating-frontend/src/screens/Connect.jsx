export default function Connect({ onConnect }) {
  return (
    <div>
      <h1>Alien Dating App 💘</h1>
      <button onClick={onConnect}>
        Connect to Alien
      </button>
    </div>
  )
}
